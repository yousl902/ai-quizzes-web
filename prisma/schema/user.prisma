model User {
    id         String  @id @default(uuid())
    first_name String
    last_name  String
    email      String  @unique
    telephone  String? @unique
    password   String
    user_quiz  UserQuiz[]
}
